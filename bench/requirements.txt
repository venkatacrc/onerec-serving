# Orchestration/benchmarking dependencies only.
# These run on the HOST (in the venv at $VENV_DIR), never inside the model
# serving containers -- no torch/CUDA dependency here on purpose.
huggingface_hub[cli]>=0.25
transformers>=4.51        # tokenizer only, for accurate prompt token counts
aiohttp>=3.9
httpx>=0.27
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
pyyaml>=6.0
rich>=13.7
tabulate>=0.9
tqdm>=4.66
